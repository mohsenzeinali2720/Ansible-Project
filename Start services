#Sample Ansible service-module.yaml
- name: Start some Services in order
  hosts: Ubuntu2
  become: yes

  tasks:
   - name: Start the database service
     service:
       name: postgresql
       state: started

   - name: Start the httpd service
     service:
       name: httpd
       state: started
